---
type: 3D Model
title: Leading-digit normalization
description: Normalizes to "3dmodel", which starts with a digit, so it is prefixed to "_3dmodel".
---

A concept whose normalized table name would start with a digit and is therefore prefixed with
an underscore.
